# Icons

Place app icons here before running `npm run tauri:build`:

- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

You can generate the set from a single source via:

```bash
npm run tauri icon path/to/source.png
```
